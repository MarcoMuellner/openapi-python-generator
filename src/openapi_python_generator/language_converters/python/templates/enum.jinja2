from enum import Enum

class {{ name }}(str, Enum):
    {% for enumItem in enum %}

    {% if enumItem[0] is string %}
    {{ enumItem[0].upper().replace("-", "_") }} = '{{ enumItem[1] }}'{% else %}
    value_{{ enumItem[0] }} = {{ enumItem[1] }}{% endif %}
    {% endfor %}
